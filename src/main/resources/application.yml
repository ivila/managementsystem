server:
  port: 8888
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:
    platform: h2
    separator: ;
  sql:
    init:
      mode: always
  jpa:
    database: h2
    show-sql: true
    #defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
  h2:
    console:
      #enabled: false
  thymeleaf:
    cache: false

